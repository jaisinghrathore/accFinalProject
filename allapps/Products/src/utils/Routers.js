import React from "react";
import { Switch, Route } from "react-router-dom";

const Routers = () => {
    return (
        <>
            <Switch>
                <h1>fsd</h1>
            </Switch>
        </>
    );
};

export default React.memo(Routers);
